# Media Studio implementation and verification — 14 September 2026

This pass rechecked the 14 findings in `media-studio-review-2026-09-14.md` against the current source and running desktop backend, then changed product source. Existing unrelated workspace changes were retained.

Playwright drives the real Media Studio component through the running Vite service and Tauri backend. Model downloads, runtime setup, inspection, imports, generation, asset storage, and exports use native commands. File dialogs receive predetermined test paths; the Windows file-picker interaction is not covered. No development service was started or restarted by this pass. The existing desktop service rebuilt automatically after native changes.

Evidence is retained in `apps/client/.cache/media-fixes-2026-09-14/`.

## Revalidation and changes

| Finding                          | Revalidation and implementation                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Saving results                | Confirmed. Added Save image, Save video, and Save SVG to Basic results and asset details, using the native verified-original export.                                                                                                                                                                                                                                                                      |
| 2. Model acquisition/components  | Confirmed. Exposed the existing FLUX model installer with size, license review, progress, cancellation, and retry. Imported KREA2 checkpoints now acquire missing Qwen components from pinned manifests with size/SHA-256 verification and partial-download recovery. Existing components are checked and reused. Unconfigured external SVG runtimes no longer advertise a nonexistent managed installer. |
| 3. Basic prompt-only video       | Confirmed. Builds an image-generation first frame connected to the video model, sharing one prompt node. Opaque video no longer adds cutout/compositing or forces transparency when submitting the first frame. Video submission compiles the saved canonical revision. Worker progress updates only the stage being executed, preserving the cached first frame.                                         |
| 4. Basic Draft                   | Confirmed. Carries sampling steps, guidance, matting, encoding, memory, dimensions, frames, and fps through Basic, conversion, persistence, and execution. New video drafts use the shared Draft preset. Existing saved choices remain editable.                                                                                                                                                          |
| 5. Damaged environment           | Confirmed. Rebuilds only the validated managed environment directory; removes the failing `uv venv --clear` dependency on an intact environment marker. Healthy setup still returns after verification.                                                                                                                                                                                                   |
| 6. Painted-mask validation       | Confirmed. Preserves floating-point values during Rust JSON parsing and uses JavaScript-compatible number serialization. Canonical graph ordering now uses Unicode values rather than locale collation, matching Rust for IDs with common prefixes and punctuation. Basic compiles the canonical saved revision before submission and retains the execution-digest check.                                 |
| 7. Failed model checks           | Confirmed. Retains bounded diagnostics and reports the process exit status when no structured failure is returned. Provides retry wording instead of directing users to correct an unspecified problem.                                                                                                                                                                                                   |
| 8. Embedding token               | Confirmed. Suggests a valid filename-derived token when metadata does not supply one, labels it Token, and requires one token before import.                                                                                                                                                                                                                                                              |
| 9. Empty compatible-model picker | Confirmed. Reference/mask controls offer the compatible model's install/setup action and open its library entry.                                                                                                                                                                                                                                                                                          |
| 10. Long-running progress        | Confirmed. Drains both worker streams continuously, retains bounded diagnostics, and reports runtime loading, sampling steps, decoding, and encoding. Basic jobs show elapsed time.                                                                                                                                                                                                                       |
| 11. Discovery/management         | Confirmed. Parses discovered Safetensors headers once without full-file hashing during discovery; reviewed import still verifies content. Added Scan models, import actions for workspace files, and native model/add-on removal controls. Direct image/video renders now hold scoped activity claims, so setup and removal can detect them.                                                              |
| 12. SVG import                   | Confirmed. Stores the sanitized vector original with its filename and vector type, retaining raster previews. Duplicate SVG imports match vector assets.                                                                                                                                                                                                                                                  |
| 13. Mask framing                 | Confirmed. FLUX mask editing now works on a bounded region and composites into the original canvas. Basic-to-Advanced video conversion retains the chosen framing, loop, and transparency.                                                                                                                                                                                                                |
| 14. Labels/context               | Confirmed. New imports retain filenames for previews, selection, search, and details; Animate image has an accessible name; video settings show WebM. Removed redundant graph implementation descriptions.                                                                                                                                                                                                |

The additional import-integrity regression suite exposed pre-existing cases where duplicate imports or direct registration accepted corrupted, missing, truncated, or non-file stored content. Publication and registration now verify stored content, and the 15 formerly disabled checks run in the regular suite.

## Verification

- Real damaged-environment repair: moved the managed `pyvenv.cfg` aside, refreshed runtime detection, and clicked **Set up Media Studio** in Playwright. Setup recreated the environment, restored dependencies, verified the AMD GPU runtime, and checked FLUX and both KREA2 checkpoints. All three image models returned to ready. Models and 91 assets remained present, including the new SVG test asset.
- SVG: imported `media-fixes-triangle.svg`; verified its SVG filter entry, filename, 160 × 120 dimensions, and rendered preview. **Save SVG** produced the sanitized SVG bytes. Reimport retained one vector asset. Filename search, a custom tag, category assignment, and deletion of this disposable test asset passed.
- Embedding: removed and freshly reimported the existing EasyNegative test embedding. The token was suggested automatically; clearing it disabled import. Restoring it allowed native registration. This does not verify embedding application during generation.
- Healthy setup reuse: invoked setup again through the real desktop command, returned to Ready, and retained a sentinel inside the environment.
- FLUX acquisition: removed its managed installation through the new UI, reviewed the 14.9 GB download, canceled a download, retried, and completed installation and automatic verification. Generated outputs remained intact.
- KREA2 components: removed the managed RedCraft checkpoint copy while retaining its source, moved the existing `chat_template.json` aside, and reimported through **Workspace files**. The native importer downloaded the missing file, verified and reused the other components, copied and registered the checkpoint, and opened its asset entry. The downloaded file matched the original SHA-256. This checks a missing-file acquisition and reuse of the remaining bundle; a completely fresh 9 GB component download was not repeated. One concurrent history refresh timed out in the test bridge; the import itself completed.
- Standalone VAE: the real Qwen VAE file was rejected as a component, with import disabled.
- Basic mask: painted the glasses, exercised undo/redo, and generated run `2c377a57-09cc-4d35-9b33-b6b5b4a61e5c` in approximately **2 minutes 56 seconds**. **Save image** in asset details produced the **832 × 1104 PNG**. Visually inspected the blue glasses and preserved portrait. Pixel comparison against the original source found **zero changed unselected pixels**; 95,104 pixels were selected, with mean selected RGB difference 26.81/255.
- Advanced KREA2 with LoRA: selected moodyKrea2Mix and Realism Enhancer in Basic, converted to Advanced, and queued run `b3c59650-09ea-4513-b780-355c7d79ab93`. It completed in **5 minutes 9 seconds**, with **12 steps**, a rank-32 adapter loaded into the denoiser at strength 1, and a **768 × 768 PNG**. Saved from Basic results and visually inspected the coherent red ceramic teapot, wooden table, and window lighting. Saved bytes match the published digest. This confirms adapter application, not a general quality comparison.
- Basic reference image: queued run `6d3c3b87-87cb-4862-b826-6f6287d7178c` using the fresh masked portrait as a Subject reference, with no mask or LoRA. It completed in approximately **1 minute 53 seconds**. Saved the **1024 × 1024 PNG** from Basic results and visually inspected the cat, blue glasses, watercolor treatment, and paper texture.
- Conversion: changed a Basic video draft to 9:16, ping-pong, and transparent, then converted to Advanced. The graph retained those choices and its Draft preset with eight sampling steps. This verifies conversion controls, not a transparent-video render. The already running opaque job retained its original settings.
- Active-generation protection: opened FLUX removal during a real image render. Removal was disabled until generation finished. **Scan models** refreshed workspace discovery.
- Basic prompt-only video: generated a FLUX first frame automatically, followed by Hunyuan run `d54fa789-7273-4612-a6ed-165304b2a51b`. The complete Basic job took approximately **10 minutes 20 seconds**, including **8 minutes 17 seconds** for animation. The request retained **8 sampling steps, 17 frames, 8 fps, 512 × 512, balanced encoding, no loop, and opaque output** while the draft was edited independently. **Save video** exported the **102,781-byte VP9 WebM**, matching the asset digest. Playwright playback advanced to 1.31 seconds with no error; duration was **2.125 seconds**. FFmpeg decoded all 17 frames. First, middle, and last frames show a coherent red teapot, moving steam, changing camera position, and no gray side bars. Mean adjacent-frame RGB difference was 2.41/255.
- Intermediate video attempts exposed and led to fixes for duplicate prompt nodes, graph ordering, progress touching a cached image node, and a callback unsupported by the installed Hunyuan pipeline. Those attempts are not counted as video successes. The successful render then exposed an incorrect sampling counter when console progress is disabled. The final worker counts sampling updates directly and separates structured events from console lines; both console modes and mixed output have regression coverage. The successful video's sampling labels preceded this final reporting-only correction.
- Final worker verification: Basic FLUX run `7d740d3b-5709-4db8-902a-8e4f815b08c5`, without references or LoRAs, completed in approximately **1 minute 45 seconds**. Its native events contain Sampling 1/4 through 4/4; Playwright recorded **Sampling 4/4 at 85%** and automatic completion. **Save image** exported a **1024 × 1024 PNG** matching the asset digest. The image has a coherent blue bowl, oranges, wood, and window light, but contains **four oranges rather than the requested three**. The submitted prompt retained the requested count; exact prompt adherence is not established. Final source and bundled worker SHA-256 match: `cfc906fc2cd6f8cbb0dc801d2df3adfc7ccf1470bf060d6f540cbc92a3239f12`.

## Regression checks

- Frontend: **41 files, 259 tests passed**. The added canonicalization fixture captures actual native execution, document, and layout digests. Related targeted reruns passed after test import corrections.
- Client core, UI, and test TypeScript checks: **passed**.
- Media TypeScript lint: **passed**.
- Managed Python: **69 tests passed**; existing Pillow deprecation warnings remain. Progress regressions use real enabled/disabled progress bars and check sampling updates, cleanup, and mixed console output.
- Native Media Studio: **196 passed, 3 ignored**. The 15 import-integrity checks and direct-generation activity claim regression are included. The ignored live setup tests are not counted as passes; the real desktop repair is documented separately.
- Final whitespace and formatting checks: **passed**. No review generation jobs remained active at completion.

## Limits

WAN rendering, embedding application, SVG generation, packaged builds, and other platforms remain unverified by this pass. The previous canceled WAN job is not treated as success or failure. Model-specific capabilities, license requirements, and hardware limits still apply. Official gated KREA downloads are not bypassed; existing local community checkpoints are used for verification. Managed model acquisition currently covers FLUX and its existing installer, plus imported KREA2's required components; acquiring video model packages still requires external files. This pass uses the existing Hunyuan package.

The original repair marker and component file are retained only as evidence; the repaired environment marker and downloaded component are in place. The temporary healthy-reuse sentinel and disposable SVG asset were removed. Existing model installations were restored after their removal/install checks. Fresh renders and saved test workflows remain available locally.

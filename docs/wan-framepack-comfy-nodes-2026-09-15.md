# WAN, FramePack and graph node verification — 15–16 September 2026

## Scope

This follow-up investigates the failures in [the loop review](video-loop-review-2026-09-15.md), [the add-on review](media-addons-review-2026-09-15.md), and [the add-on fixes](media-addons-fixes-2026-09-15.md). Evidence is retained in [the local review directory](../apps/client/.cache/wan-framepack-nodes-2026-09-15/), which is ignored by Git.

The tested system is Windows 11 25H2, an AMD Radeon RX 9070 with 16 GiB VRAM, and 32 GiB system RAM. The managed environment uses PyTorch 2.12.0+rocm7.14.0, Diffusers 0.39.0, Transformers 5.13.0 and Safetensors 0.8.0. The installed AMD driver is Adrenalin 26.7.1. Other GPUs and operating systems are not established by these results.

## Results

- **WAN generation:** the matched painting and original fire examples no longer have the colored-block corruption after correcting the sampling canvas and repeated-endpoint conditioning. Rectangular delivery now preserves proportions. Crossfade still fails on the square painting and the 17- and 33-frame fire examples; ping-pong passes. A separate wide painting preview crossfade passes on retained fresh frames.
- **FramePack:** fixed loading stalls, GPU image-encoder staging and two timing defects. Fresh generation is coherent, and its corrected crossfade passes checks and four inspected playback crossings. Ping-pong still reverses a blink and has a brief eye-open flash.
- **Graph nodes:** added Canny edges, Depth map, Apply ControlNet, Image to mask and Combine masks. Native SD1.5 generation and masked editing work with verified dependencies. These are operations in Machdoch's graph, not arbitrary ComfyUI workflow compatibility.
- **Preservation:** fresh native Basic/Advanced Pixel M plus EasyNegative outputs match the previous PNG bytes, including all eight embedding vectors. The saved Hunyuan loop remains continuous. Python, native and client media checks pass.

## WAN investigation

The earlier 50-step, guidance-5 and CPU-decoding controls were used as evidence, rather than treating crossfade or video encoding as the origin of the corruption.

- A fresh 384×384, 17-frame, 20-step, guidance-1 render reproduces severe colored blocks and bands before blending or encoding. This diagnostic uses upstream first-frame conditioning, seed 9152026, the previous Monet source and `Painting process step by step.` No LoRA is loaded. The first frame is recognizable; subsequent generated frames are corrupted. [Raw frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-reproduction/raw-frames.jpg), [request](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-reproduction/request.json), [video](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-reproduction/video/output-0000.webm).
- The captured first denoising call was repeated on the CPU with the same inputs. Its mean relative difference from the GPU output was approximately 0.59%; both were finite. This bounds one call, not every later activation. [Measurements](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-reproduction/denoiser-cpu-comparison.json).
- CPU prompt encoding and GPU prompt encoding remained finite, with similar output spread and approximately 6.25% mean relative difference. Patch convolution, rotary positions and conditioning projections also received bounded CPU/GPU comparisons. These checks do not establish full numerical equivalence or validate every model operation. [Prompt comparison](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-reproduction/prompt-cpu-comparison.json), [operation comparisons](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-operation-evidence.json).
- A complete fresh render using mathematical scaled dot-product attention still has severe corruption. Selecting that backend is therefore not a confirmed fix. [Raw frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-math-attention/raw-frames.jpg), [video](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-math-attention/video/output-0000.webm).
- FP32 versus BF16 time-projection comparisons at five timesteps differed by approximately 0.14–0.15% on average. No precision change was promoted to a production fix on that evidence alone. [Measurements](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-conditioning-evidence.json).
- Crossfade and ping-pong reprocessing of the corrupted attention-control frames are rejected for motion discontinuity near frame 1. Blending does not repair the sampled content. [Loop evidence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-math-attention/loop-review.json).

The earlier report already checked the installed WAN checkpoint and configuration against publisher revision `b8fff7315c768468a5333511427288870b2e9635`. The installed VAE also contains the [upstream patch-layout fix](https://github.com/huggingface/diffusers/pull/12041). A [similar ComfyUI report on an RX 9070 XT](https://github.com/Comfy-Org/ComfyUI/issues/11574) supplies no verified cause or remedy for this installation. Neither observation proves a GPU defect.

The [publisher example](https://huggingface.co/Wan-AI/Wan2.2-TI2V-5B-Diffusers) uses a larger resolution. Two 1280×704 controls failed before producing frames: one with Windows exception `0xC0000005`, and an isolated retry with `HIPBLAS_STATUS_ALLOC_FAILED` during VAE image encoding. A third control with 128-pixel VAE tiles, 17 frames, 20 steps and guidance 5 completed in 1,975 seconds. Its fresh frames preserve the Monet painting without the earlier colored blocks and bands. It uses diagnostic upstream first-frame conditioning. These settings do not reproduce the publisher's full 121-frame, 50-step example. [Frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-publisher-resolution-small-tiles/raw-frames.jpg), [last raw frame](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-publisher-resolution-small-tiles/frames/016.png), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-publisher-resolution-small-tiles/result.json).

A matched 384×384 control retained the same checkpoint, source, prompt, seed, 17 frames, 20 steps, guidance 5, upstream conditioning and 128/96 VAE tiles. It completed in 1,705 seconds and still produced severe colored blocks and bands in its raw frames. Smaller VAE tiles therefore do not resolve the low-resolution corruption. The comparison supports the generation canvas as the relevant change; the production square-canvas render separately checks normal endpoint conditioning. [Matched small frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-small-matched-tiles/raw-frames.jpg), [last raw frame](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-small-matched-tiles/frames/016.png), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-small-matched-tiles/result.json).

Both loop modes reject this matched small control for motion discontinuity near frame 1. Continuous Chrome playback decoded 85 frames without seeking or dropped frames; all four captured crossings show the corrupted tail resetting to the source painting. [Loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-small-matched-tiles/loop-review.json), [playback crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-small-matched-raw/four-crossings.jpg).

The publisher's [TI2V size configuration](https://github.com/Wan-Video/Wan2.2/blob/main/wan/configs/__init__.py) lists 1280×704 and 704×1280; its [image-to-video implementation](https://github.com/Wan-Video/Wan2.2/blob/main/wan/textimage2video.py) derives a canvas from the source aspect ratio and a 704×1280 pixel budget. Machdoch's small preview canvases use substantially less area.

The larger control's raw video decoded through five continuous Chrome playback cycles: 85 frames, no seeking, no dropped frames, and four complete captured crossings. Crossfade reprocessing still rejects an abrupt visual change near frame 2; ping-pong passes the encoder checks. Raw generation quality and loop acceptance are separate findings. [Playback crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-large-raw/four-crossings.jpg), [loop evidence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-publisher-resolution-small-tiles/loop-review.json).

Worker 1.69.0 separates WAN sampling dimensions from delivery dimensions. It samples on a 32-pixel-aligned canvas with at least a 704×1280 pixel area, then resizes to the requested output dimensions. GPUs below 24 GiB use 128-pixel VAE tiles with a 96-pixel stride; the larger profile retains 256/192. A separate reprocessing check passes both loop modes when the coherent larger frames are delivered at 512×288. This check reuses captured frames rather than performing new denoising. [Preview loop evidence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-large-delivery-preview/loop-review.json).

The first production square-canvas render completed in 1,944 seconds with worker 1.70.0: 960×960 sampling, 384×384 delivery, and the existing two-endpoint latent lock. The large colored bands disappeared, but frames 1–12 have excessive mosaic-like texture and jump back to the clean endpoint. This is a **quality failure**, including before resizing. All 17 delivered frames exactly match Lanczos resizing of their captured native frames. Crossfade rejects uneven cadence at both sizes; ping-pong's technical pass does not repair the texture damage. [Delivered frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-native-canvas/raw-frames.jpg), [native middle frame](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-native-canvas/native-frames/007.png), [native/delivery correspondence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-sampling-output/provenance.json), [loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-native-canvas/loop-review.json).

Worker 1.71.0 uses first-frame conditioning when the supplied endpoints are identical. Loop assembly then operates on the generated sequence without a forced terminal latent frame. The [upstream TI2V implementation](https://github.com/huggingface/diffusers/blob/main/src/diffusers/pipelines/wan/pipeline_wan_i2v.py) also uses first-frame conditioning; the more general `last_image` parameter does not implement a TI2V last-frame lock. Machdoch's graph supplies the first image again when no separate endpoint is connected, so this correction also applies to ordinary single-image animation.

The matched fresh production comparison completed in 1,855 seconds: the same 960×960 sampling canvas, 384×384 delivery, source, prompt, seed, 17 frames, 20 steps, guidance 5 and VAE tiles. Only the repeated-endpoint policy changed. All 17 raw frames are coherent, including the native middle frame; the mosaic damage and forced clean tail are gone. Every delivered frame exactly matches Lanczos resizing of its native frame. This confirms two generation defects on this example: the undersized sampling canvas, and the extra terminal latent lock for repeated endpoints. Distinct-endpoint conditioning remains a separate, unverified path. [Fresh frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-first-anchor/raw-frames.jpg), [native middle frame](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-first-anchor/native-frames/007.png), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-first-anchor/result.json), [native/delivery correspondence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-first-anchor-native/provenance.json).

**The short square painting crossfade remains rejected.** Reprocessing at 384×384, standard 512×512 delivery and native 960×960 fails the appearance check near frame 2. At 384×384, another interior transition has an appearance ratio of 1.55 and the blend contains uneven motion around its direction changes. The file boundary alone passes. The checks were not relaxed, and the rejected video is retained only for inspection. Ping-pong passes. Continuous Chrome playback captured all four crossings of the raw, inspection-only crossfade and ping-pong videos, with 85, 45 and 160 decoded frames respectively, no seeking and no dropped frames. These captures show coherent painting content; they do not turn the rejected crossfade into a confirmed loop fix. [Loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-production-first-anchor/loop-review.json), [512 preview checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-first-anchor-preview-512/loop-review.json), [crossfade inspection](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-first-anchor-crossfade-inspection/four-crossings.jpg), [ping-pong crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-first-anchor-pingpong/four-crossings.jpg).

Diagnostic cubic and cosine easing curves also fail on the square painting at all three sizes. Both retain passes on the corrected FramePack and the wide WAN preview controls, but neither fixes the remaining WAN example. No curve change was promoted to production. [Curve comparisons](../apps/client/.cache/wan-framepack-nodes-2026-09-15/crossfade-curve-probe.json).

The original fire example was also repeated with worker 1.71.0, retaining `Flames coming up`, seed 0, eight steps, guidance 5, 17 frames and 8 fps. It samples at 1280×736 and delivers 512×288. The fresh run completed in 1,131 seconds. All raw frames retain coherent car, flame and smoke detail, without the earlier blocks and bands. Native/delivery correspondence passes for all 17 frames. This confirms the generation correction on the original fire source as well as the painting. [Fire frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-production/raw-frames.jpg), [native middle frame](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-production/native-frames/008.png), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-production/result.json), [correspondence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-native/provenance.json).

The 17-frame fire crossfade is still rejected near frame 2. The measured 3.696 mean pixel change is already present between source frames 8 and 9, which the assembly copies unchanged; their original neighboring changes are 2.034 and 2.204. Blending reduces the following change to 1.935, raising the appearance ratio to 1.862. Ping-pong passes. Raw, inspection-only crossfade and ping-pong playback each captured four complete crossings with no seeking or dropped frames. The fire/smoke content remains coherent, but the rejected crossfade is not counted as a working delivery. [Loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-production/loop-review.json), [source/blend measurements](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-blend-transition-evidence.json), [crossfade captures](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-fire-crossfade-inspection/four-crossings.jpg), [ping-pong captures](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-fire-pingpong/four-crossings.jpg).

A fresh 33-frame, 16-fps fire run retained the same source, prompt, seed, eight steps and guidance. It completed in 1,317 seconds. The raw sequence avoids the block corruption but shows some vehicle geometry drift. Increasing the frame count does **not** resolve crossfade: after final delivery processing, the 17-frame crossfade is rejected near frame 7, with velocity-change ratio 1.768 against the unchanged 1.5 limit. Ping-pong delivers 64 frames and passes its intended-reversal checks; it does not establish natural forward-moving fire. [All raw frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-33-production/raw-frames.jpg), [native middle frame](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-33-production/native-frames/016.png), [generation result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-33-production/result.json), [final loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-33-final-delivery/loop-review.json).

The rectangular render exposed a delivery defect introduced by sampling-grid rounding: resizing 1280×736 directly to 512×288 slightly changes proportions. Worker 1.72.0 uses proportional Lanczos delivery with a centered crop of the excess canvas. A square-marker regression reproduces the old 64×62 output and verifies 64×64 after correction, in landscape and portrait orientations. Production delivery and loop checks were repeated on both fire renders and the square painting's captured native frames. All 17 painting frames remain pixel-identical. The fire crossfade failures remain; the geometry correction is not a loop-quality remedy. [Geometry measurements](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-geometry-evidence.json), [final 17-frame fire delivery](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-final-delivery/result.json), [final 33-frame delivery](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-33-final-delivery/result.json), [unchanged painting pixels](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-painting-final-delivery/result.json).

Final raw, inspection-only crossfade and ping-pong videos for both fire runs each completed five continuous Chrome playback cycles, with all four crossings captured, no seeking and no dropped frames. The final 17-frame source decoded 85/45/160 frames across those three checks; the 33-frame source decoded 165/85/320. Captures and the complete assembled crossfade were inspected. The file-boundary captures do not override the interior rejections. [Playback summary](../apps/client/.cache/wan-framepack-nodes-2026-09-15/final-wan-playback-summary.json), [17-frame crossfade inspection](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-fire-final-crossfade-inspection/four-crossings.jpg), [33-frame crossfade inspection](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-fire-33-final-crossfade-inspection/four-crossings.jpg), [33-frame assembled crossfade](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-fire-33-final-delivery/loop-crossfade/assembled-frames.jpg).

During verification, `C:\Development\machdoch\models` was no longer present. No relocated WAN 2.2 package was found in the checked model/cache locations. The same pinned revision was restored under `D:\Models\machdoch\wan-review\models\wan-2.2-ti2v-5b`. All 20 files match their publisher content identities, and native discovery recognizes the package as ready in that D: workspace. The new control uses the restored package. [Hashes](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-restored-package-verification.json), [native discovery](../apps/client/.cache/wan-framepack-nodes-2026-09-15/wan-native-discovery.json).

## FramePack investigation

FramePack was assessed independently. The previous report's image and add-on results did not establish that its video failures had been fixed.

The missing package was restored under `D:\Models\machdoch\framepack-review\models\framepack-i2v-hy` from these pinned sources:

| Component                                    | Repository                                                                 | Revision                                   |
| -------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------ |
| Text encoders, tokenizers, VAE and scheduler | [HunyuanVideo](https://huggingface.co/hunyuanvideo-community/HunyuanVideo) | `e8c2aaa66fe3742a32c11a6766aecbf07c56e773` |
| FramePack transformer                        | [FramePackI2V_HY](https://huggingface.co/lllyasviel/FramePackI2V_HY)       | `86cef4396041b6002c957852daac4c91aaa47c79` |
| Image encoder                                | [flux_redux_bfl](https://huggingface.co/lllyasviel/flux_redux_bfl)         | `45b801affc54ff2af4e5daf1b282e0921901db87` |

The reproduction uses the original two dragon images and motion prompt, 640×640, 25 requested frames, 12 steps, guidance 9, 16 fps and seed 72526043. [Original operation](../apps/client/.cache/video-loop-review-2026-09-15/framepack-source-operation.json), [fresh corrected request](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-temporal-aligned/request.json).

The first two fresh attempts stalled in prompt weight loading before denoising. Stack samples located the work inside Transformers' tensor materialization and FP16-to-BF16 conversion, with heavy Windows page faults. Disabling asynchronous loading alone still incurred substantial loading pressure and was not retained as the solution.

The replacement loader reads Safetensors tensors through the `pread` backend, converts one tensor at a time, and validates the complete parameter set against the model and shard index. FramePack transformer loading uses the same file-reading backend. The isolated worker loads its companion by absolute file path. Tests verify every parameter and prompt output on a sharded model and exercise the actual isolated-process import. These changes address loading; visual generation and loop acceptance are assessed separately.

That attempt passed prompt loading but spent several minutes per endpoint in the existing AMD CPU image-encoder path. It then crashed at the first denoising step with Windows exception `0xC0000005`; the native stack includes the CPU allocator and tensor conversion. This attempt produced no new frames. A concurrent desktop rebuild also failed, and the native bridge subsequently became unavailable. The trace does not establish the underlying cause of the access violation. [Log](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-bounded-load-fixed-import.log).

The image encoder now uses staged GPU execution and returns to CPU afterward. AMD retains FP32 image-encoder weights. With the existing convolution backend configuration, the actual Siglip patch convolution agreed with CPU to approximately `4.18e-7` mean relative error. This bounded probe does not by itself establish full video quality. [Probe](../apps/client/.cache/wan-framepack-nodes-2026-09-15/siglip-patch-evidence.json).

The restored package's large weight files match their pinned publisher hashes. The remaining small configuration, index and tokenizer files also match the Git blob identities from their respective pinned source repositories. The existing FP8 cache format was prepared locally to avoid repeated transformer conversion during the next render. [Package verification](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-package-verification.json), [configuration verification](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-config-verification.json), [cache preparation](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-cache-preparation.json).

Native model discovery recognizes the restored package as ready when the workspace root is `D:\Models\machdoch\framepack-review`. The direct render uses that package explicitly. The `C:\Development\machdoch` workspace does not automatically discover models under the separate D: workspace. The restored package and its FP8 cache occupy approximately 68.7 GB. [Discovery result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-native-discovery.json).

The cached, staged-GPU reproduction completed in 4,758 seconds: approximately 558 seconds for model loading and prompt encoding, followed by denoising and decoding. All 37 decoded section frames and the 25 selected output frames were retained. Visual inspection shows a coherent dragon blinking and moving, without WAN's colored-block corruption. This confirms that the loading and image-encoder changes can complete real generation on the tested system. Crossfade still rejects an interior motion discontinuity near frame 3; ping-pong passes the encoder checks. [Fresh frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-gpu-encoder-cached/raw-frames.jpg), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-gpu-encoder-cached/result.json), [loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-gpu-encoder-cached/loop-review.json).

A separate timing defect was identified in the low-memory VAE configuration. Its six-sample-frame stride is divided by the VAE's four-frame temporal compression when advancing latent tiles, but the decoder still emits six frames per chunk. A test using the installed [Diffusers temporal decoder](https://github.com/huggingface/diffusers/blob/v0.39.0/src/diffusers/models/autoencoders/autoencoder_kl_hunyuan_video.py) reproduces the resulting timeline distortion: a 37-frame linear trajectory ends ten frames early. The corrected four-frame stride reproduces the complete trajectory exactly. Worker 1.68.0 applies this correction, with a regression test covering the configured memory profiles. [Timing evidence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-temporal-tiling-evidence.json).

The 12–23 GiB profile also uses 128-pixel spatial tiles instead of 64-pixel tiles. On the tested 16 GiB adapter, a nine-frame, 640×640 decode probe completed in 25.9 seconds with 3.01 GB peak allocation. This probe used random latents to measure memory and dimensions; it is not evidence of generation quality. Smaller GPUs retain 64-pixel spatial tiles, and the larger-GPU profile retains its existing 256-pixel tiles. All profiles now use temporal strides aligned to the VAE's compression. [Probe](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-128-tile-profile.json).

The corrected fresh render completed in 1,769 seconds with coherent dragon frames. Its 37 decoded section frames and generated latents were retained. A second decode used exactly those latents and the same 128-pixel spatial tiles, changing only the temporal stride back to six. The old decoder's last frame most closely matches corrected frame 26: it loses about ten frames of the trajectory, confirming the timing defect on actual generated content. The last-frame comparison at 160×160 has mean RGB error 0.318 against corrected frame 26, versus 3.971 against corrected frame 36. [Fresh frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-temporal-aligned/raw-frames.jpg), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-temporal-aligned/result.json), [same-latent comparison](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-same-latent-timeline-comparison.jpg), [measurements](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-same-latent-timeline-comparison.json).

Crossfade still rejected the corrected 25-frame output. A second timing defect came from nearest-frame selection: reducing 37 frames to 25 alternated one- and two-frame source intervals. At the rejected transition, neighboring motion remained aligned while the speed ratio reached 1.87. Worker 1.70.0 replaces that selection with uniform temporal resampling, linearly blending adjacent frames at fractional timestamps while preserving both endpoints. A trajectory regression test catches the previous uneven intervals. The final production resampling and encoding functions were repeated on the fresh decoded frames; crossfade and ping-pong both pass, with 13 and 48 output frames respectively. No additional denoising was needed for this postprocessing correction. [Final frames](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-final-timing/raw-frames.jpg), [loop checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-final-timing/loop-review.json), [crossfade video](../apps/client/.cache/wan-framepack-nodes-2026-09-15/framepack-final-timing/loop-crossfade/output-0000.webm).

Continuous Chrome playback of the final crossfade and ping-pong videos decoded 65 and 240 frames respectively: five cycles, no seeking, no dropped frames, and all four last-two/first-two crossings captured and viewed. The crossfade joins are visually continuous. Ping-pong reverses the blink and retains a brief eye-open flash; its encoder pass does not establish natural facial motion. Linear resampling can also soften fast motion. These results establish the tested short dragon workflow, not arbitrary actions or longer FramePack sections. [Final crossfade crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/framepack-final-crossfade/four-crossings.jpg), [final ping-pong crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/framepack-final-pingpong/four-crossings.jpg).

The earlier reproduction's raw playback had a pose reset, and its rejected crossfade retained an interior transition. Those baseline captures remain available for comparison. A Chrome harness failure caused by the 100 MB DevTools message limit was resolved by transferring videos in smaller chunks. [Baseline raw crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/framepack-raw-before/four-crossings.jpg), [baseline crossfade crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/framepack-crossfade-before/four-crossings.jpg).

## ComfyUI node coverage

Machdoch uses its own typed graph, native executor and Diffusers workers. It does not run a ComfyUI server or import arbitrary ComfyUI workflow JSON. The additions below implement the relevant node operations in that graph; they do not install or execute entire custom-node packs.

Selection combines workflow relevance with available adoption evidence. Canny and depth ControlNet appear in [ComfyUI's ControlNet documentation](https://docs.comfy.org/tutorials/controlnet/controlnet). The [ControlNet auxiliary preprocessors](https://github.com/Fannovel16/comfyui_controlnet_aux), [KJNodes](https://github.com/kijai/ComfyUI-KJNodes), [Impact Pack](https://github.com/ltdrdata/ComfyUI-Impact-Pack), and [VideoHelperSuite](https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite) have substantial repository adoption. Repository stars indicate interest in packs; they are not measurements of individual node usage. A defensible per-node usage ranking was not available.

| ComfyUI operation or family                                       | Previous Machdoch coverage                    | Change / scope                                                                                                                           |
| ----------------------------------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Model loading, CLIP prompts, sampling, LoRA and textual inversion | Existing local generation and add-on paths    | Retained; regression checks include Pixel M and all eight EasyNegative vectors                                                           |
| Image input, crop, resize, color, sharpen and composite           | Existing native image operations              | Retained                                                                                                                                 |
| IP-Adapter, OpenPose, selection and mask grow/feather/invert      | Existing supported model paths                | Retained                                                                                                                                 |
| Canny / CannyEdgePreprocessor                                     | Missing reusable edge preparation             | Added **Canny edges**, with integer low/high thresholds                                                                                  |
| DepthAnythingV2Preprocessor                                       | Missing depth preparation                     | Added **Depth map**, using Depth Anything V2 Small                                                                                       |
| ControlNetApplyAdvanced for edges/depth                           | Existing OpenPose path only                   | Added **Apply ControlNet**, with strength and start/end controls; one Canny or depth control for local SD1.5 generation or editing       |
| ImageToMask                                                       | Missing reusable channel extraction           | Added **Image to mask**, with alpha, luminance or RGB channels, inversion, and an optional same-sized reference image for imported masks |
| MaskComposite                                                     | Missing mask combination                      | Added **Combine masks**, with soft add, subtract and multiply; both masks must refer to the same image                                   |
| Upscale models and video delivery                                 | Existing Spandrel tiling and video/loop paths | Retained                                                                                                                                 |

New templates generate from edges or depth. Invalid control ranges, incompatible models, mismatched ports, and simultaneous pose plus edge/depth controls are rejected before generation. Model dependencies have pinned revisions, sizes and SHA-256 checks; the packaged Python resource list includes the new workers. The existing managed environment supplies OpenCV, Transformers and Safetensors.

The live desktop palette exposes all five additions. Reviewing an eleven-node editing graph exposed overlapping image cards and preprocessing nodes placed after the generator. Automatic and template layout now follow graph connections with sufficient card spacing. The live graph measured zero overlapping node rectangles. [Palette](../apps/client/.cache/wan-framepack-nodes-2026-09-15/node-palette-ui.png), [graph](../apps/client/.cache/wan-framepack-nodes-2026-09-15/mask-controlnet-graph-ui.png), [measurements](../apps/client/.cache/wan-framepack-nodes-2026-09-15/graph-layout-measurement.json).

Remaining scope excludes arbitrary ComfyUI JSON/custom-pack execution, stacked ControlNets, SDXL/FLUX/video ControlNet variants, mask offsets and the other MaskComposite operators, RIFE interpolation, VACE, and additional detector/preprocessor models. This work does not claim that every popular node is supported.

### Real node workflows

Both initial native workflows use the previously verified SD1.5 checkpoint, 512×512, 24 steps, guidance 7.5 and seed 9152026. Control strength is 1, active from 0 through 0.85. The source is the earlier Pixel M/EasyNegative castle image; the new generation loads no add-ons.

| Workflow                                       | Native run                             | Visual result                                                                                                                                                                                                                                                                                                                                                      |
| ---------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Image → Canny → ControlNet → generation → save | `9c224d99-287d-44f1-85c5-427d39d57c68` | Coherent castle; the generated edges follow the supplied outline. [Guide](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-canny-0.png), [output](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-canny-1.png), [native result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-canny-result.json)                              |
| Image → depth → ControlNet → generation → save | `92e47c13-c231-4647-8b64-e6f6df4ab02d` | Coherent castle; depth preserves the main composition while allowing different surface detail. [Guide](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-depth-0.png), [output](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-depth-1.png), [native result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-depth-result.json) |

All seven files for the Canny ControlNet, depth ControlNet and depth estimator were downloaded and checked against the pinned sizes and SHA-256 values. [Dependency evidence](../apps/client/.cache/wan-framepack-nodes-2026-09-15/control-model-download-evidence.json), [manifest](../apps/client/src-tauri/src/media/controlnet_components.json).

The native runs reused these verified dependencies. A fresh automatic download through the native dependency installer was not separately exercised. The first Canny/depth runs recorded worker version 1.66.0; the subsequent direct worker checks below use 1.67.0.

### Editing and regression checks

- **Control strength comparison:** the same prompt, seed and sampling with Canny strength zero produced a different, coherent castle composition. With strength one, the source's tower outline and composition are retained. This confirms a visible conditioning effect; image difference alone is not treated as a quality score. [Zero-strength output](../apps/client/.cache/wan-framepack-nodes-2026-09-15/direct-canny-zero/images/output-0000.png), [request and result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/direct-canny-zero/).
- **Mask editing:** isolated workflow workers extracted red and green masks from an imported RGBA fixture and multiplied their soft values. Their combined mask and a newly generated Canny guide fed the production SD1.5 editing worker. The initial 28-step edit changed detail but did not deliver the requested white/blue colors. A 40-step edit with full edit/mask strength, ControlNet strength 0.2 through 0.35, and an explicit blue-castle prompt produced visibly blue architecture. The complete native graph subsequently passed as run `86e859c2-e26b-4497-8d84-1fdf3527e53a`, including preprocessing, both channel masks, multiplication, controlled editing and saving. Visual inspection confirms blue architecture. Every pixel outside the combined mask remained identical; 100,742 selected pixels changed, with a mean selected-pixel channel difference of 66.54. [Base](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-canny-1.png), [combined mask](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-mask-blue-combined.png), [native output](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-mask-blue-output.png), [native result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-mask-blue-result.json), [pixel checks](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-mask-blue-pixel-evidence.json).
- **LoRA and embedding preservation:** rerunning the earlier 28-step Pixel M plus EasyNegative request produced the exact previous PNG bytes, SHA-256 `42274da25ec1a16e8de781a8edb3cffc2195fe1460e1ae8afd2262e3bdf8cd61`. All eight EasyNegative vectors remain recorded. [Output](../apps/client/.cache/wan-framepack-nodes-2026-09-15/direct-addon-regression/images/output-0000.png), [result](../apps/client/.cache/wan-framepack-nodes-2026-09-15/direct-addon-regression/result.json).
- **Native Basic/Advanced parity:** fresh runs `4e15e757-77e9-4d22-99b0-49778a38463e` and `9208fd1c-b934-42aa-8cfc-8f090c5cc0d3` repeated the earlier 20-step Pixel M/EasyNegative requests with worker 1.70.0. Both match each other and the previous PNG bytes: `84d415ddb2f5525a799b9e3ba98e7c3d87bbb8d10c0dc7a9feac588454b7d16d`. All eight vectors appear in both encoded negative prompts. The fresh image was also viewed. [Image](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-addon-final-basic.png), [summary](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-addon-final-summary.json), [native requests/results](../apps/client/.cache/wan-framepack-nodes-2026-09-15/native-addon-regressions-final.json). These replay saved Basic/Advanced submissions through native commands; they do not repeat the UI click sequence.
- **Continuous video playback:** Chrome decoded five concatenated copies of each clip without browser seeking or dropped frames. All four last-two/first-two crossings were captured. The baseline WAN reproduction visibly resets from corrupted blocks to the source painting; the previously verified Hunyuan teapot loop remains visually continuous. This rechecks the saved Hunyuan video, not a fresh Hunyuan generation. [Baseline WAN crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/wan-raw/four-crossings.jpg), [Hunyuan crossings](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/hunyuan-loop-preserved/four-crossings.jpg), [playback records](../apps/client/.cache/wan-framepack-nodes-2026-09-15/playback/).
- **Final model readiness:** SD1.5 and FLUX.2 both passed native offline loading probes with worker 1.72.0. Their model catalog entries report ready. The refreshed editing graph has no preflight errors and enables Run. This is a load/readiness check for FLUX.2, not a new FLUX.2 LoRA render. [Probe results](../apps/client/.cache/wan-framepack-nodes-2026-09-15/model-readiness-172.json), [catalog](../apps/client/.cache/wan-framepack-nodes-2026-09-15/catalog-verified-172.json), [desktop readiness](../apps/client/.cache/wan-framepack-nodes-2026-09-15/final-graph-readiness-172.json).

## Automated checks

- Python media: **145 passed**, including the sharded FramePack loader, isolated import, temporal timeline preservation and resampling, WAN sampling canvases, first-frame conditioning, VAE tiles and delivery geometry, channel masks and soft mask arithmetic. Repeated with worker 1.72.0.
- Native media: **230 passed, four ignored**. The ignored cases require external integration fixtures or specialized execution; they are not counted as passed.
- Client media: **367 passed** across 54 files, repeated after the graph layout correction.
- Core, UI and test TypeScript checks passed after the graph layout correction.
- Media lint passed.

The native suite was repeated after adding first-frame WAN response validation, including crossfade. One intermediate run failed the descendant-process timeout test, which used a 1.5-second marker timer and could report a child that wrote its marker before cleanup finished. The isolated retry passed. The fixture now waits for a release file written after timeout cleanup returns, and verifies that the child actually started. The complete repeated suite passed with this stronger check. Production process termination was unchanged. Core, UI and test TypeScript checks also passed after the conditioning contract update.

Logs are retained in the evidence directory. The four ignored native cases cover external add-on/dependency fixtures, reviewed BiRefNet assets, and two isolated runtime installation/repair tests. They were not run as part of this verification.

## Remaining limits and configuration

- The square painting and both fire crossfades remain rejected. More frames and alternative easing curves did not supply a verified remedy. Distinct-endpoint WAN conditioning and the separate cyclic-latent Seamless mode are not established by the successful first-frame renders.
- WAN and FramePack real renders use their production Python generation functions with observational frame capture. Loop rechecks use production assembly, encoding and decoding functions. These video checks do not repeat native graph publication or the full desktop video click sequence.
- Restored WAN and FramePack packages reside in the D: review workspaces documented above. Native discovery recognizes them there. The removed `C:\Development\machdoch\models` directory was not recreated, and its workspace does not discover the D: packages automatically. Image models and the new control dependencies remain ready in the managed model library.
- The full publisher WAN recipe, longer FramePack sections, portrait/ultrawide video renders, fresh video LoRA combinations, other GPUs and memory profiles, Linux/CUDA execution, fresh Hunyuan/LTX generation and packaged release installation remain untested. The successful 16 GiB runs do not establish these combinations.
- Crossfades can morph details, and FramePack's linear temporal resampling can soften fast motion. The inspected examples establish their specific results, not natural motion for arbitrary prompts.
